name: Parkalot Test Run

on:
  workflow_dispatch:

jobs:
  test-booking:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Print Hello
        run: echo "Hello from GitHub Actions!"
